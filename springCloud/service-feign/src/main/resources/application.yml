eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/  #注册中心
server:
  port: 8765
spring:
  application:
    name: service-feign   #本服务的命名

feign:
  hystrix:
    enabled: true  #开启断路器